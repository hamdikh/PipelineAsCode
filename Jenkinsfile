pipeline{
    agent any

    stages{
        stage('Init'){
            echo "Init Stage"
        }

        stage('Build'){
            echo "Build Stage"
        }

        stage('Deploy'){
            echo "Deploy Stage"
        }

    }

}